import 'package:flutter/material.dart';

class TraningProfileWidget extends StatelessWidget {
  const TraningProfileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Training profile "));
  }
}
